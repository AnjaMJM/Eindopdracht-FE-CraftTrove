import ErrorMessage from "../../components/ErrorMessage/ErrorMessage.jsx";

function NotFound() {

    return (
            <ErrorMessage message="Whoops! Either you lost track, or the page you're looking for does not exist" />
    );
}

export default NotFound;